# Contract Tests

This directory contains language-independent semantic tests for the ASTER contract gate.

The gate covers finite/non-finite numerical semantics, explicit financial periods, opaque resource identifiers, and lifecycle tombstones. Runtime-specific TypeScript/Python projections must remain compatible with these semantics.
