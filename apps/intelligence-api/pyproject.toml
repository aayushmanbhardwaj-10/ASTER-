[project]
name = "aster-intelligence-api"
version = "0.1.0"
description = "ASTER internal Intelligence Plane API"
requires-python = ">=3.12,<3.13"
dependencies = [
  "fastapi>=0.115,<1.0",
  "uvicorn[standard]>=0.30,<1.0",
]

[tool.ruff]
line-length = 100

[tool.pytest.ini_options]
addopts = "-q"
