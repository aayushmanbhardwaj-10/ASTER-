P1.2 Semantic Formula Contracts are frozen in architecture/PHASE-1.2-SEMANTIC-FORMULA-CONTRACTS.md.

This note records the freeze decision: formula variants use ontology-defined fidelity ranking with evidence gating; temporal bindings are semantic rather than string offsets; flow/stock behavior and aggregation requirements are explicit; rollups are conditional; sign semantics are explicit and reversible; semantic expressions are non-executable; algebraic equivalence requires explicit solution variants; and material formula definitions are versioned and provenance-bound.

Next architecture gate: P1.3 Ontology Validation.